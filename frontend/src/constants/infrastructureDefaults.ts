// default form values for infrastructure dialogs. mirrors the backend
// defaults in app/core/constants.py so create dialogs prefill with the
// same numbers the persistence layer would otherwise inject.

export const DEFAULT_BUFFER_DISTANCE = "5";
export const DEFAULT_GLIDE_SLOPE_ANGLE = "3.0";
export const DEFAULT_GLIDE_SLOPE_ANGLE_TOLERANCE = "0.1";
export const DEFAULT_LHA_SETTING_ANGLE = "3.0";
export const DEFAULT_RADIUS = "3";
export const DEFAULT_LHA_SPACING = "3";
