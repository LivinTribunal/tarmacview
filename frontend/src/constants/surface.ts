/** default taxiway width in meters when no width is stored. */
export const DEFAULT_TAXIWAY_WIDTH_M = 20;

/** default runway width in meters when no width is stored. */
export const DEFAULT_RUNWAY_WIDTH_M = 45;
