// shared list pagination constants. single source for the default page size
// and the "load all" fetch ceiling used across list pages and api calls.

/** default rows per page in list views. */
export const DEFAULT_PAGE_SIZE = 10;

/** max items fetched for "load all" list calls. */
export const MAX_LIST_LIMIT = 200;
